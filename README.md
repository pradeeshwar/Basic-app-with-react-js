Basic login and dashboard drawer using react naive (expo)
1)Download the zip file
2)Go the project directory
3)run the following commands:
       npm install,
       expo install react-native-gesture-handler,
       npm start.
